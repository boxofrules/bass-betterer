# --- Proprietary: NEVER commit plaintext IRs or built binaries (they embed the IRs) ---
ir/*.wav
ir/*.bin
*.component/
*.vst3/
*.aaxplugin/
*.zip

# --- Private maintainer notes (the capture recipe) — kept local, never committed ---
CLAUDE.md
*.private.md

# Local reference material dropped in the repo root (forum/Reddit saves, etc.)
/*.pdf

# Build output
build/
build-*/
cmake-build-*/
*.dSYM/
*.a
*.o

# JUCE FetchContent / dependency checkouts
_deps/

# IDE
.idea/
.vscode/
*.xcodeproj/
*.xcworkspace/
DerivedData/

# macOS
.DS_Store

# Local secrets / signing keys / IR passphrase
*.p12
*.cer
*.key
.env*

# User's local validation snapshot — never committed
/validate-0.2.0/
